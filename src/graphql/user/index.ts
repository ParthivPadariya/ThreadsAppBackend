import {typeDef} from './typedef'  
import {queries} from './queries'
import {mutations} from './mutation'
import {resolver} from './resolvers'  

export const User = { typeDef, queries, mutations, resolver}